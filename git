git add . 把修改的代码放到暂存区
git commit -m "修改说明"     注意有冒号代码提交本地仓库
git push 推送到远程仓库
 

当你开始一个新项目或加入别人的项目时：

git init: 在当前文件夹初始化一个新的 Git 仓库。
git clone [url]: 从远程仓库（如 GitHub/GitLab）克隆一个项目到本地。

2. 基础循环（修改与提交）
git status —— 查看哪些文件修改了、哪些还没
git add . —— 把所有修改过的文件都放到“待提交”清单里。
git commit -m "提交说明" —— 给这次修改存个档，说明你改了什么。
git log --oneline —— 用简洁的一行模式查看历史记录。

3. 远端同步（推送与拉取）
当你需要和云端代码同步时：
git pull —— 把远程最新的代码下载并合并到你的本地。
git push —— 把你本地的提交上传到远程仓库。
git fetch —— 只看远程有什么更新，但不自动合并（对应你图中那个箭头指着横杠的按钮）。

4. 分支管理（并行开发）
分支能让你在不破坏主线代码的情况下尝试新功能：
git branch —— 看看本地有哪些分支，当前在哪个。
git checkout -b [分支名] —— 新建一个分支并立刻跳过去。
git merge [分支名] —— 把指定分支的代码合并到当前分支。

5. 救急命令（后悔药）
如果写错了或者想回退：

撤销暂存: git reset HEAD [文件名] —— 把 add 进去的文件撤出来。

回退版本: git reset --hard [commit_id] —— 彻底回到之前的某个版本（慎用，会丢弃未保存的改动）。

暂时存放: git stash —— 代码写了一半想换个分支，先用这个把改动“藏”起来，晚点用 git stash pop 拿回来。